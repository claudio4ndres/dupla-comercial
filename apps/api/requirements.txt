# Dependencias de RUNTIME del backend (sin las de dev/test) para la imagen de
# producción. Reflejan `pyproject.toml` → [project.dependencies]. uvicorn[standard]
# se incluye porque el contenedor lo usa como servidor ASGI.
fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.40
pydantic-settings>=2.5
httpx>=0.27
pyjwt>=2.9
cryptography>=48.0.0
openpyxl>=3.1.5
python-pptx>=1.0.2
# Secret Manager (SECRETOS_BACKEND=gcp en prod): persiste los refresh tokens de
# Gmail/Drive fuera de /tmp, así no se pierden en cold-starts ni deploys.
google-cloud-secret-manager>=2.20
